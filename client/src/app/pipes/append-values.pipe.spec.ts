import { AppendValuesPipe } from './append-values.pipe';

describe('AppendValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
