services:
  frontend:
    build:
      context: ../..
      dockerfile: infra/local/frontend.Dockerfile
    ports:
      - "4200:4200"
    volumes:
      - ../../client:/app
      - /app/node_modules
    command: npm start
    environment:
      - CHOKIDAR_USEPOLLING=true

  backend:
    build:
      context: ../..
      dockerfile: infra/local/backend.Dockerfile
    ports:
      - "7071:7071"
    volumes:
      - ../../api:/home/site/wwwroot
      - /home/site/wwwroot/node_modules
      - /home/site/wwwroot/dist
    command: bash -c "npm run watch & func start --csharp"
    environment:
      - AzureWebJobsStorage=UseDevelopmentStorage=true
      - FUNCTIONS_WORKER_RUNTIME=node
