{
  "name": "manitoba-master-bowlers-api",
  "version": "1.0.0",
  "description": "Azure Functions backend for Manitoba Master Bowlers",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "npx func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.4.0",
    "auth0": "^4.1.0",
    "express-oauth2-jwt-bearer": "^1.6.0",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "20.x",
    "azure-functions-core-tools": "^4.0.5537",
    "rimraf": "^5.0.0",
    "typescript": "^4.9.5"
  },
  "engines": {
    "node": "20.x"
  }
}
