# Manitoba Master Bowlers

## Project layout
- `client/` – Angular app (run `npm install` and `npm start` here).
- `api/` – Azure Functions backend.
- Root configs: repo-level `.gitignore`, `.editorconfig`, CI workflow files.

## Getting started
- Frontend: `cd client && npm install && npm start`
- Backend: `cd api && npm install && npm start`
